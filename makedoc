#!/bin/sh
rm -rf doc
mkdir doc
bin/lunadoc lunamark.lua lunamark/*.lua lunamark/*/*.lua
for prog in lunamark lunadoc; do
  bin/lunamark -t man -s -Xall -dtitle=$prog,section=1,date="`date`",description="$prog" $prog.1.txt > $prog.1
done

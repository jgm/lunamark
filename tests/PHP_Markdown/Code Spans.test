markdown --strict
<<<
From `<!--` to `-->`
on two lines.

From `<!--`
to `-->`
on three lines.

>>>
<p>From <code>&lt;!--</code> to <code>--&gt;</code> on two lines.</p>

<p>From <code>&lt;!--</code> to <code>--&gt;</code> on three lines.</p>
